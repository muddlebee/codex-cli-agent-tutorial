# m2 - Todo Planning Layer

## What was added
- `TodoManager` persisted at `.nano-agent/sessions/<id>/todo.json`
- `/todo` chat commands for add/start/done/list
- Prompt injection for current todo state

## Constraints
- Max 20 todos
- Only one `in_progress` item at a time
