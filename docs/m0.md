# m0 - Codex Exec Adapter

## What was added
- `CodexExecProvider` that wraps `codex exec --json`
- JSONL parsing and normalization into internal `RuntimeEvent`
- CLI command: `nano-agent run "<task>"`

## Why this milestone exists
This creates a working control plane over Codex without implementing model/tool orchestration directly.
